<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100">
  <!-- Tarka: one disc, cut once, halves slid out of true.
       Geometry mirrors frontend/src/components/Mark.jsx — change both together.
       Literal colours rather than currentColor: a favicon has no page to
       inherit from. Light ground so the mark still reads against dark
       browser chrome, where an ink tile would disappear. -->
  <rect width="100" height="100" rx="22" fill="#ECEDEF"/>
  <g fill="#14161A" transform="translate(50 50) scale(0.78) translate(-50 -50)">
    <path d="M14 46 A30 30 0 0 1 74 46 Z"/>
    <path d="M26 54 A30 30 0 0 0 86 54 Z"/>
  </g>
</svg>

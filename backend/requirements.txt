# Agentic Workflow Framework
langgraph==0.1.4      # Core graph orchestration
langchain==0.2.5      # Component wrappers and tool abstractions
langchain-anthropic==0.1.23 # Interface for Claude / structured bindings
# NOTE: langchain-anthropic must stay <0.2. Later releases require
# langchain-core>=0.3, which conflicts with langgraph 0.1.4 / langchain 0.2.5.

# Specialized Search SDKs
tavily-python==0.8.0  # Clean web claims for agents (needs >=0.7 for search_depth="fast")
requests==2.32.5      # Direct OpenAlex REST calls in paper_scout_open_alex

# Environment & Schema Validation
python-dotenv==1.0.1  # Handling credentials cleanly (.env)
pydantic       # Restricting LLM node output schemas

# High-Performance Web Server
fastapi==0.111.0      # Framework for SSE streaming API
uvicorn==0.30.1       # ASGI server execution
